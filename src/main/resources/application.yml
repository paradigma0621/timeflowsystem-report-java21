spring:
  application:
    name: report
  datasource:
    url: jdbc:postgresql://localhost:5432/reportdb
    username: repuser
    password: 123456
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 0 # intentionally 0 to use a random free port

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

mq:
  queues:
    clockings-registers-queue: clockings-registers
    #queue-2: other-queue
    #queue-3: another-queue